import React from 'react';
import ReactDOM from 'react-dom';
import App from './covid-19/App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
